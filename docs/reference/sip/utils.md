# The `utils` module

This module provides some utility function needed when dealing with GAMS bags.

::: gamslib.sip.utils
    options:
        members:
            - count_bytes
            - count_files
            - fetch_json_schema
            - is_bag
            - md5hash
            - sha512hash