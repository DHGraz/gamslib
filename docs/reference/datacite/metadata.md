# The `metadata` module

This module defines the central metadata model and re-exports key metadata classes.

```python
from gamslib.datacite.metadata import Metadata
```

::: gamslib.datacite.metadata.Metadata
