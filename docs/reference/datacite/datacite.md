# The DataCite class

This is the main class of a Invenio RDM conform DataCite object. It uses a bunch of subclasses.

The DataClass class is definied in the datacite_record module, but made available via
the datacite package directly:

```python
from gamslib.datacite import DataCite
```

::: gamslib.datacite.datacite_record.DataCite